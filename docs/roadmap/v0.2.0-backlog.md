# CoCro v0.2.0 — Backlog

> **Status : ✅ TERMINÉ** — Toutes les tâches v0.2.0 ont été livrées.
> Prochaine release cible : **v0.3.0** (voir [v0.3.0-audit.md](./v0.3.0-audit.md))

---

## Session Management

- [x] **Lister les sessions de l'utilisateur** — `GET /api/sessions/mine` (sessions créées ou jointes)
- [x] **Supprimer / archiver une session** — `DELETE /api/sessions/{code}` (créateur uniquement)
- [x] **Page "Mes sessions"** — `MySessionsComponent` avec cartes par status, actions rejoindre/supprimer
- [x] **Fix multi-session heartbeat** — supprimé le reverse lookup `user:{userId}:session` mono-session, utilise maintenant le `shareCode` des session attributes WebSocket. Voir [roadmap détaillée](./multi-session-heartbeat-fix.md)
- [x] **Nettoyage heartbeat à la transition ENDED** — `CheckGridUseCase` supprime les clés heartbeat Redis de tous les participants et désactive le cache quand la session passe en `ENDED`
- [x] **Limite de sessions actives par utilisateur** — max 5 sessions PLAYING/INTERRUPTED (`SessionLimitRule`, guards dans Create/Join)
- [x] **Expiry des sessions INTERRUPTED** — `InterruptedSessionExpiryScheduler` supprime après 7 jours d'inactivité (daily cron 3h)
- [x] **Permettre à tout utilisateur de rejoindre une session INTERRUPTED** — le code le permettait déjà, docs corrigés (lifecycle.md, session-ddd.md)
- [x] **Participants visibles avec username & statut** — afficher la liste des participants (nom, statut JOINED/LEFT, rôle créateur) dans le composant de jeu. Voir [spec détaillée](./participants-username-spec.md)

## Grid Management

- [x] **Lister les grilles de l'utilisateur** — `GET /api/grids/mine`
- [x] **Page "Mes grilles"** — `MyGridsComponent` avec hub, édition, lancement de session
- [x] **Éditer une grille existante** — endpoint `PATCH /api/grids` + wiring frontend
- [x] **Supprimer une grille** — `DELETE /api/grids/{shortId}` (créateur uniquement, guard sessions actives) + bouton delete dans `MyGridsComponent`

## UX / Flow

- [x] **Grid picker dans le lobby** — remplacé par le flow "Lancer" depuis "Mes grilles"
- [x] **Redirect post-création de grille** — après `POST /api/grids`, redirection vers "Mes grilles"
- [x] **Fix lien cassé** dans `create-session.component.html` : `routerLink="/grids/editor"` → `/grid/create`
- [x] **Sidebar collapsible** — sous-menus Grilles (éditeur, mes grilles) et Sessions (créer, rejoindre, mes sessions)

## Robustesse WebSocket

- [x] **Retry `POST /sync`** sur réception de `SyncRequired` avec backoff exponentiel (1s → 2s → 4s)
- [x] **Gap detection** — si `GridUpdated.revision > clientRevision + 1`, déclenche automatiquement un `POST /sync`

## Domain / Refactoring

- [x] **Author value object** — `Author(id, username)` dans `Session` et `Grid` (remplace `creatorId` UUID brut)
- [x] **GridDimension value object** — `GridDimension(width, height)` dans `GridMetadata`
- [x] **GlobalClue optionnel** — `GlobalClue?` dans le domain `Grid`
- [x] **Title dans GridMetadata** — déplacé du top-level de `Grid` vers `metadata`

---

## Hors scope (v0.3.0+)

- Système d'invitation par lien (feature "invite")
- Historique des parties
- Scores / leaderboard
