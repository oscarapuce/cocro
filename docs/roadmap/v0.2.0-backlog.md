# CoCro v0.2.0 — Backlog

> Version actuelle : **0.1.0 (MVP)**
> Prochaine release cible : **0.2.0**

---

## Session Management

- [ ] **Lister les sessions de l'utilisateur** — `GET /api/sessions/mine` (sessions créées ou jointes)
- [ ] **Supprimer / archiver une session** — `DELETE /api/sessions/{code}` (créateur uniquement)
- [ ] **Limite de sessions actives par utilisateur** — ex. max 5 sessions PLAYING/INTERRUPTED
- [ ] **Expiry des sessions INTERRUPTED** — scheduler qui archive/supprime après N jours d'inactivité
- [ ] **Permettre à tout utilisateur de rejoindre une session INTERRUPTED** (pas seulement previously-LEFT) — actuellement seuls les ex-participants peuvent revenir

## Grid Management

- [ ] **Lister les grilles de l'utilisateur** — `GET /api/grids/mine`
- [ ] **Éditer une grille existante** — endpoint `PATCH /api/grids` existe déjà côté BFF, wiring frontend manquant
- [ ] **Supprimer une grille**

## UX / Flow

- [ ] **Grid picker dans le lobby** — remplacer le champ texte `gridId` par un sélecteur "Mes grilles"
- [ ] **Redirect post-création de grille** — après `POST /api/grids`, rediriger vers le lobby avec la nouvelle grille pré-sélectionnée
- [ ] **Fix lien cassé** dans `create-session.component.html` : `routerLink="/grids/editor"` → `/grid/create`

## Robustesse WebSocket

- [ ] **Retry `POST /sync`** sur réception de `SyncRequired` avec backoff exponentiel (1s → 2s → 4s)
- [ ] **Gap detection** — si `GridUpdated.revision > clientRevision + 1`, déclencher automatiquement un `POST /sync` (client a manqué des events)

---

## Hors scope (v0.3.0+)

- Système d'invitation par lien (feature "invite")
- Historique des parties
- Scores / leaderboard
