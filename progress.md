Original prompt: [$develop-web-game](/Users/oscar_mallet/.codex/skills/develop-web-game/SKILL.md) Make an audit of my grid editor, forr the design and the UX of the app, If possible generate a grille de mots croisés en français de 8x10 , then make tests , what do you think is missing , make a prioritized list

2026-03-18
- Started audit of Angular grid editor with focus on visual design, interaction model, and test coverage.
- Confirmed there was no existing progress.md.
- Identified main implementation area under cocro-angular/src/app/presentation/features/grid/editor.
- Identified existing Playwright coverage in cocro-angular/e2e/grid-editor.spec.ts.
- Design charter already flags grid editor as a weak point that bypasses shared design system.
- Next steps: run the app, inspect the editor visually, run E2E tests, assess feasibility of generating an 8x10 French crossword grid, and produce a prioritized gap list.
- Angular dev server initially failed in sandbox due local bind restrictions, then failed compilation on strict TypeScript because several `error: (err)` callbacks used implicit `any`.
- Applied minimal build-unblocker by typing those error callbacks as `unknown` in landing, home, auth, and lobby screens so the app can compile for audit/testing.
- Ran the Angular app locally and captured desktop/mobile screenshots of `/grid/create` through Playwright with fake localStorage auth.
- Visual audit findings: editor is usable but still visually inconsistent with the design charter, mobile layout is not adapted, and the letter editor shows an unfinished placeholder (`future number component`).
- Manual interaction check found a UX issue in selection/direction behavior: because cell (0,0) is preselected, clicking it immediately can flip direction before typing, making first-entry behavior surprising.
- Existing Playwright suite `e2e/grid-editor.spec.ts` passes fully (17/17), but coverage is mostly structural and misses responsive, accessibility, text-entry, direction, submit-feedback, and data persistence scenarios.
- Generated one 8x10 fill candidate from the local French wordlist; it proves the dictionary can fill a grid, but the result includes obscure entries, so it is not publication-ready without curation/ranking.
- Follow-up styling pass requested by user:
- Restored green clue styling in rendered clue cells and arrows.
- Reworked grid editor, clue editor, and letter editor toggle states to use stronger active/hover/focus contrast and avoid the washed-out active label issue from the prior radial effect.
- Hardened `cocro-card` dark variant so text and form controls render white on forest-green cards.
- Verified visually on `http://127.0.0.1:4201/grid/create` with a filled clue screenshot (`MER`) and checked DOM state that `Indice simple` remains the active toggle after selection.
- Added a shared croco-yellow-green token family and applied it consistently to clue rendering and cell toggles.
- Relaxed spacing in the shared landing/home shell, removed inline color styling from landing/home templates, and made right-panel CTA buttons stay horizontal on desktop.
- Updated the app shell/sidebar so clicks on sidebar navigation emit a collapse request; verified after clicking a sidebar link that the sidebar ends in `auth-sidebar--collapsed` and the burger reports `aria-expanded=\"false\"`.
- Sidebar collapse/expand now animates more smoothly with width, padding, opacity, and transform transitions coordinated with the shell grid column transition.
- Removed the clue gradient in the grid so clue cells now use a flat croco background with black borders, black arrows, and black clue text for a cleaner print-like look.
- Updated grid letter typography away from the Comic Neue feel toward the display face with stronger ink contrast.
- Added content-area click-to-collapse behavior in the root shell; verified that clicking inside the main content while the sidebar is open leaves the sidebar in `auth-sidebar--collapsed`.
- Strengthened the mini-grid hero shadow so it reads clearly toward the bottom-right instead of appearing offset toward the top-left.
- Switched grid letter cells to plain Arial with normal weight for a more neutral, less playful feel.
- Moved the grid editor tool stack to the left side of the board by reordering the main editor layout.
- Nudged the hero mini-grid leftward and normalized the outer border thickness using an explicit 1px border.
